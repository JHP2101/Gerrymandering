git lfs install
git lfs track track "*.shp"
git add . gutattributes
cd the path of the directory
git init
git add US_state_2010.shp
git sommit -m "Upload Shape File"
git remote add origin **
# Confirm the origin
git remote -v
git push -u origin master
